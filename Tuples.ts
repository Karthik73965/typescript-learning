//this was the normal array 
const array :number[] = []

// tuples - basically we are declaring before using it 
const array1 :[string, number , boolean]= ["kartik", 2, true]

// through type 
type array3 = [string , number]

const array2 :array3 = ["karthik", 1]

// this was a use full thing in tyep script to strictly wright the things up 